# Server configuration
PORT=3000
SESSION_SECRET=change-me-to-a-long-random-string
NODE_ENV=development

# Admin bootstrap credentials
ADMIN_EMAIL=admin@volonterka.org
ADMIN_PASSWORD=ChangeMe123!

# Absolute path on the host where uploads (proofs, media) are stored
# Defaults to <project>/public when not set
UPLOAD_ROOT=
